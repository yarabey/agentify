#!/usr/bin/env bash
# install.sh — ЗАГЛУШКА тикета 0.1.
#
# Назначение (бизнес): «установка агента одной командой» (FR C1): скрипт должен
# выкачивать бинарь агента из GitHub Releases (собранный GoReleaser) и ставить
# демон (launchd/systemd). Реальная реализация — в тикете EPIC агента/релиза.
#
# Здесь только каркас, чтобы структура монорепо (§5) была полной.
set -euo pipefail

echo "agentify install.sh: placeholder (ticket 0.1) — implemented later"
exit 0
