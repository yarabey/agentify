// Package hello — временный hello-world пакет агента.
//
// Назначение (бизнес): первичный каркас агента-демона (FR C1, C5), нужный,
// чтобы тикет 0.1 имел собираемый и тестируемый код до появления WebSocket,
// outbox и провайдеров в последующих тикетах.
//
// Как устроено (тех): единственная чистая функция Greeting без внешних
// зависимостей; будет вытеснена реальными пакетами агента позже.
package hello

// ServiceName — каноническое имя сервиса-агента для логов и приветствия.
const ServiceName = "agent"

// Greeting возвращает приветственную строку сервиса-агента.
//
// Заглушка тикета 0.1: даёт make test реальный предмет проверки.
func Greeting() string {
	return "hello from " + ServiceName
}
