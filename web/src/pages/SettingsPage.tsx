/**
 * Заглушка экрана настроек (`/settings`). Назначение: место для будущего
 * управления профилем/уведомлениями/сессиями (FR группы G/I) — полноценная
 * реализация запланирована в тикете **9.6**. Здесь только заголовок, чтобы
 * shell-тест (тикет 9.1) мог проверить, что маршрут рендерится без ошибок.
 */
export function SettingsPage(): JSX.Element {
  return (
    <section>
      <h1 className="text-2xl font-bold">Настройки</h1>
      <p className="mt-2 text-muted-foreground">
        Экран настроек будет реализован в тикете 9.6.
      </p>
    </section>
  );
}
