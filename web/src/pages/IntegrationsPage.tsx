/**
 * Заглушка экрана интеграций (`/integrations`). Назначение: место для
 * будущего списка/подключения машин-агентов пользователя (FR B*) —
 * полноценная реализация запланирована в тикете **9.3**. Здесь только
 * заголовок, чтобы shell-тест (тикет 9.1) мог проверить, что маршрут
 * рендерится без ошибок.
 */
export function IntegrationsPage(): JSX.Element {
  return (
    <section>
      <h1 className="text-2xl font-bold">Интеграции</h1>
      <p className="mt-2 text-muted-foreground">
        Экран интеграций будет реализован в тикете 9.3.
      </p>
    </section>
  );
}
