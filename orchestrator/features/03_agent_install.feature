# language: ru
#
# Источник: docs/User_stories_Gherkin.md §3 «Подключение машины разработки»
# (текст сценариев скопирован без изменений). Trace: FR C1-C3.
#
# Помечена целиком @manual: install.sh — однострочная установка на "чистую"
# macOS/Ubuntu (интерактивный опрос адреса оркестратора/UUID/провайдера,
# systemd-автозапуск) — не HTTP/WS-поверхность оркестратора, а
# provisioning реальной ОС. Степов для этих сценариев в
# orchestrator/internal/bddsteps намеренно нет (undefined steps в godog не
# провалили бы сборку, но осмысленно проверить тут нечего без реальной
# машины) — @manual исключён из тегового фильтра по умолчанию (см.
# orchestrator/features/README.md). Реальная приёмка этой цепочки —
# install/test-install.sh, гоняемый CI-джобой agent-install-test (тикет 4.2)
# внутри чистого docker run ubuntu:24.04, и agent-service-test (тикет 4.4)
# для systemd-автозапуска/рестарта — оба уже покрывают приёмку FR C1-C5
# отдельно от этого BDD-харнесса.
@manual
Функция: Установка агента одной командой
  Как пользователь
  Я хочу развернуть агента на машине одной командой
  Чтобы быстро подключить новую машину

  Сценарий: Установка на чистой системе
    Дано чистая macOS или Ubuntu
    Когда я выполняю однострочную команду установки
    Тогда скрипт ставит всё необходимое
    И интерактивно спрашивает адрес оркестратора, UUID и провайдера
    И после настройки агент работает в фоне

  Сценарий: Пользователь выбирает провайдера сам
    Дано идёт интерактивная настройка
    Когда меня спрашивают про провайдера ИИ
    Тогда я выбираю Claude или Claude Code и указываю свои учётные данные провайдера
    И система не управляет оплатой провайдера
