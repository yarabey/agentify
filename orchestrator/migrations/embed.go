// Package migrations встраивает SQL-миграции схемы оркестратора в бинарь.
//
// Назначение (бизнес): оркестратор должен сам приводить схему БД в актуальное
// состояние при старте (тикет 0.3 «goose-миграции применяются на старте»), без
// внешнего шага деплоя и без отдельного образа с goose CLI. Это даёт
// воспроизводимый локальный и прод-запуск: подняли контейнер оркестратора — он
// применил недостающие миграции и только потом начал отвечать на /healthz.
//
// Как устроено (тех): каталог содержит goose-миграции 0000N_*.sql (источник
// правды схемы — их правит EPIC 1/2/5), а этот файл встраивает их в бинарь
// через embed.FS. Сам прогон миграций живёт в пакете
// orchestrator/internal/migrate, который читает FS отсюда и применяет её к
// Postgres через библиотеку pressly/goose. SQL-файлы здесь НЕ редактируются в
// рамках тикета 0.3 — только встраиваются.
package migrations

import "embed"

// FS — встроенная файловая система с goose-миграциями оркестратора.
//
// Содержит все 0000N_*.sql из этого каталога. Передаётся в migrate.Apply как
// источник миграций; goose сам отбирает и сортирует файлы по номеру версии.
//
//go:embed *.sql
var FS embed.FS
